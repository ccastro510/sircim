{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Bitacora</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Biaccion</th>
                <td>{{ entity.biAccion }}</td>
            </tr>
            <tr>
                <th>Bifecha</th>
                <td>{{ entity.biFecha|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('bitacora') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('bitacora_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('bitacora_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
