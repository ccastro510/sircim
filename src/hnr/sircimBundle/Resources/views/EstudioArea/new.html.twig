{% extends 'hnrsircimBundle::Layout.html.twig' %}

{% block body -%}
    <h1>Agregar estudio</h1>

    <form action="{{ path('estudioarea_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('estudioarea') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
