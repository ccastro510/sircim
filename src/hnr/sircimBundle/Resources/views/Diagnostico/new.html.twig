{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Diagnostico creation</h1>

    <form action="{{ path('diagnostico_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('diagnostico') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
