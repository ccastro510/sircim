{% extends 'hnrsircimBundle::Layout.html.twig' %}

{% block body -%}
    <h1>Cita list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ciedadpaciente</th>
                <th>Ciestado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cita_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ciEdadPaciente }}</td>
                <td>{{ entity.ciEstado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cita_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cita_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cita_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
